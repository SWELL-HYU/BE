# --- API framework & ASGI ---
fastapi==0.121.0
fastapi-cli==0.0.16
uvicorn[standard]==0.38.0
python-multipart==0.0.9  # 파일 업로드(multipart/form-data) 지원

# --- Database ---
sqlalchemy==2.0.44
sqlalchemy2-stubs==0.0.2a38
psycopg2-binary==2.9.9 # PostgreSQL 드라이버

# --- Migrations ---
alembic==1.13.2

# --- Configuration & validation ---
python-dotenv==1.2.1
pydantic==2.12.3
pydantic-settings==2.6.0
email-validator==2.1.1  # Pydantic EmailStr 타입을 위한 이메일 검증 라이브러리

# --- Security ---
PyJWT==2.9.0

# --- AI & imaging ---
google-genai==0.3.0
Pillow==10.4.0

# --- Async file I/O ---
aiofiles==24.1.0

